require("tmh.options")     -- sane defaults
require("tmh.remap")       -- change the default keymaps
require("tmh.lazy")        -- load plugin manager
require("tmh.colorscheme") -- set colorscheme after plugins are loaded

require("config.mappings") -- load which-key mappings
